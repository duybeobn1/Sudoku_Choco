# Ignore compiled output
*.class

# Ignore Java dependency directories (like those for IDEs)
/lib/
/vendor/

# Explicitly ignore the problematic JDK directory (as identified in the error)
oracleJdk-25.jdk/